#include <iostream>
using namespace std;

int main() {
    int age = 70;
    int ageAtLastExam = 16;
    bool isNotIntoxinated = true;

    if((age >= 1) && (age < 16)) {
        cout << "You can't drive" << endl;
    } else if (! isNotToxinated) {
        cout << "you can't drive" << endl;
    } else {
        cout << "you can drive" << endl;
    }

    return 0;
}